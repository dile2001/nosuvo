# Flask Web Framework
flask==3.0.0
flask-cors==4.0.0

# NLP and AI
spacy==3.7.2
openai==2.0.0

# Authentication
authlib==1.3.0
PyJWT==2.10.1
cryptography==44.0.0

# Utilities
python-dotenv==1.0.0
requests==2.32.3

# PostgreSQL Database (REQUIRED)
psycopg2-binary==2.9.9

# Database Migration Tools
alembic==1.13.1

# PostgreSQL Requirements:
# - PostgreSQL 9.6+ (minimum)
# - PostgreSQL 17.x (recommended for production)
# - PostgreSQL 18.x (latest)
# - Python 3.6+ (required for Alembic)

# Post-install commands:
# 1. Install spaCy model: python -m spacy download en_core_web_sm
# 2. Create PostgreSQL database: createdb nosuvo_db
# 3. Set up .env.local with your database credentials


